// Single canonical House Account. Providers with no active rep (imported
// unmatched, or deactivated) are owned here. It's an admin profile under the
// hood (so only admins can access/order), but surfaced everywhere as one
// "House Account" — never per-admin.
export const HOUSE_ACCOUNT_OWNER_ID = "6634d855-e406-47be-800a-8c470eb23806";
export const HOUSE_ACCOUNT_LABEL = "House Account";
