# Use a hoisted node_modules layout (like npm) so Expo Router and other
# React Native tooling that assumes flat dependency resolution can find
# their entry points. pnpm's default symlinked layout breaks
# `expo-router/entry` resolution on iOS.
node-linker=hoisted
